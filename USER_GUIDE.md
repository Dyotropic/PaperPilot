# PaperPilot 用户手册

> 面向课题攻关的可解释智能文献工作流系统
>
> 版本：v2 Phase 3 | 最后更新：2026-08-29

---

## 目录

1. [项目简介](#1-项目简介)
2. [准备工作](#2-准备工作)
3. [安装步骤](#3-安装步骤)
   - [3.4 配置 AI 模型服务（API Key）](#34-配置-ai-模型服务api-key)
   - [3.5 高级选项字段说明](#35-高级选项字段说明)
   - [3.6 配置 OpenAlex API Key（建议配置）](#36-配置-openalex-api-key建议配置)
4. [首次启动](#4-首次启动)
5. [功能详解](#5-功能详解)
   - [5.1 检索论文](#51-检索论文)
   - [5.2 管理文献库](#52-管理文献库)
   - [5.3 AI 精读与对话](#53-ai-精读与对话)
   - [5.4 阅读原文](#54-阅读原文)
   - [5.5 导出文献](#55-导出文献)
   - [5.6 设置页面](#56-设置页面)
6. [常见问题](#6-常见问题)

---

## 1. 项目简介

PaperPilot 是一款**桌面端学术文献管理工具**，帮助你完成从"课题输入"到"文献阅读"的完整工作流：

- 输入研究课题 → 自动提取关键词 → 从 arXiv 和 OpenAlex 检索论文 → 智能排序打分 → 保存到文献库 → AI 精读分析 → 导出 BibTeX/CSV

**核心亮点：**

- **智能关键词提取**：AI 自动从课题描述中提取核心技术术语，支持手动拖拽归类（主/副/普通三层）
- **多源检索**：同时搜索 arXiv（预印本）、OpenAlex（正式发表论文）和 Europe PMC（生物医学+最新预印本），自动去重
- **语义精排**：使用 Cross-Encoder 模型对检索结果进行语义相关性排序
- **AI 精读**：调用 DeepSeek API 对论文全文进行结构化分析（核心贡献 / 方法 / 证据 / 亮点 / 局限）
- **PDF 阅读器**：内置 PDF.js 阅读器，支持文字选择、搜索、页码导航、夜间模式
- **AI 对话助手**：课题上下文感知的学术对话助手，支持 Markdown 富文本渲染

---

## 2. 准备工作

### 硬件要求

| 项目 | 最低配置 | 推荐配置 |
|------|---------|---------|
| 操作系统 | Windows 10+ | Windows 11 |
| 内存 | 8 GB | 16 GB+ |
| 磁盘 | 5 GB 可用空间 | 10 GB+（含模型缓存） |
| 网络 | 首次启动需联网 | 稳定网络 |

### 需要你事先安装的软件

| 软件 | 用途 | 下载地址 |
|------|------|---------|
| **Python 3.10 ~ 3.12** | 运行环境 | https://www.python.org/downloads/ |
| **Git**（可选） | 克隆仓库 | https://git-scm.com/download/win |

> **安装 Python 时务必勾选 "Add Python to PATH"**，否则后续命令行会找不到 Python。

### 需要你提前准备的东西

- **DeepSeek API Key**（推荐）：用于关键词提取、AI 精读和对话助手。注册地址：https://platform.deepseek.com/
  - 如果不打算使用 AI 功能，软件也可以离线运行（关键词提取会回退到本地算法）
- **OpenAlex API Key**（建议配置，非必须）：免费申请，可把 OpenAlex 检索额度从每天 100 次提升到 10 万次，避免检索被限流。详见 [§3.6](#36-配置-openalex-api-key建议配置)
- **GitHub 账号**（可选）：用于克隆仓库和提交反馈

---

## 3. 安装步骤

以下步骤假设你使用的是 Windows 系统。下面出现的命令请在 **命令提示符（cmd）**或 **PowerShell** 中执行。

### 3.1 下载项目代码

**方式一：Git 克隆（推荐）**

打开命令行，进入你想存放项目的目录（例如 `E:\`），执行：

```bash
git clone https://github.com/Dyotropic/PaperPilot.git
cd PaperPilot
```

**方式二：直接下载 ZIP**

1. 在浏览器打开 https://github.com/Dyotropic/PaperPilot
2. 点击绿色的 **Code** 按钮 → **Download ZIP**
3. 解压到你想要的目录（例如 `E:\PaperPilot`）

### 3.2 创建虚拟环境（推荐但可选）

虚拟环境可以隔离这个项目的依赖，不让它和系统其他 Python 项目冲突。

```bash
python -m venv .venv
.venv\Scripts\activate
```

激活成功后，命令行前面会出现 `(.venv)` 标识。

### 3.3 安装依赖

```bash
pip install -r requirements.txt
```

这会安装以下主要依赖：

| 包名 | 用途 |
|------|------|
| flet | 桌面 UI 框架 |
| sqlalchemy | 数据库 |
| faiss-cpu | 向量检索 |
| sentence-transformers | 语义模型 |
| keybert | 英文关键词提取 |
| arxiv | arXiv API 客户端 |
| pymupdf | PDF 文本提取 |
| pywebview | PDF 独立阅读窗口 |
| jieba | 中文分词 |
| beautifulsoup4 + lxml | HTML 解析 |
| pyyaml | 配置文件解析 |

> **安装时间**：首次安装约需 3–8 分钟（含 PyTorch 自动下载）。如果下载慢，可以配置 pip 镜像：
> ```bash
> pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
> ```

> **关于 PDF 自动下载**：程序会优先使用 `curl_cffi`（更强的浏览器指纹伪装）访问出版商网站获取 PDF。如果 `curl_cffi` 未安装，程序会自动降级为普通 HTTP 请求，不影响基本使用。如需安装：
> ```bash
> pip install curl_cffi
> ```

### 3.4 配置 AI 模型服务（API Key）

PaperPilot 的所有 AI 功能（关键词提取、中英翻译、AI 精读、打分、StudyCopilot 对话）都依赖一个 **LLM 服务商**。你可以在「设置」页的「AI 模型服务」卡片中完成全部配置，无需手改文件；以下也给出 config.yaml 对应的写法作为参照。

**config.yaml 对应结构（新增 `llm:` 节）：**

```yaml
llm:
  provider: deepseek       # 服务商名，见下表
  api_key: sk-xxxxxxxxxxxx  # 该服务商的 API Key（Ollama 可留空）
  base_url: ""              # 留空用内置默认地址，一般不用填
  model: deepseek-v4-flash  # 主模型
  score_model: ""           # 高级：精排打分专用模型，可留空
  chat_model: ""            # 高级：对话专用模型，可留空
  reasoning_model: ""       # 高级：两步推理模型，留空=关闭两步推理
```

> 旧版本只有 `deepseek:` 配置节的，可以继续使用：程序会自动把旧的 Key/Model 读进来，无需迁移。

**推荐：直接在界面上配置**。打开软件 →「设置」页 →「AI 模型服务」卡片：

1. **Provider（服务商）**：下拉选择你要用哪一家（共 7 家，见下表）。
2. **模型**：下拉选择该家的模型；列表里没有的可选「自定义…」并手动输入模型名。
3. **API Key**：粘贴对应服务商的密钥（见下表如何获取）。密钥只在本地 `config.yaml` 存储，不会上传。
4. 点 **「测试」** 验证连通性 → 点 **「保存」** 生效（下次 AI 调用时应用）。

> 切换 Provider 后，Key 和模型都会重置为未保存状态，需要重新填写并保存。

**各服务商 API Key 获取方式 + 内置模型：**

| Provider | 注册/获取 Key | Key 特点 | 内置默认模型 |
|----------|---------------|----------|--------------|
| **DeepSeek** | https://platform.deepseek.com → 注册 → 「API Keys」→ 创建 | `sk-` 开头 | deepseek-v4-flash / deepseek-v4-pro |
| **OpenAI** | https://platform.openai.com → 「API keys」→ 创建 | `sk-` 开头 | gpt-5.6-sol、gpt-5.6-terra、gpt-5.6-luna、gpt-5.5 |
| **Anthropic(Claude)** | https://console.anthropic.com →「API Keys」→ 创建 | `sk-ant-` 开头 | fable-5、opus-5、opus-4.8、opus-4.7、opus-4.6 |
| **智谱 GLM** | https://open.bigmodel.cn → 注册 →「API 密钥」→ 新建 | 长字符串 | glm-5.3、glm-5.2 |
| **Kimi(Moonshot)** | https://platform.moonshot.cn → 注册 →「API 密钥」 | `sk-` 开头 | kimi-k3 |
| **通义千问(Qwen)** | https://dashscope.console.aliyun.com →「API-KEY」 | `sk-` 开头 | qwen-plus、qwen-turbo、qwen-max |
| **Ollama（本地）** | 本地安装 Ollama 后**无需 Key，留空即可** | 无需 | qwen2.5:7b（可自选本地已拉取的模型） |

> 各家模型名随时可能变化，「自定义…」可直接填任何官方最新模型名，不必受内置列表限制。可用「测试」验证模型名是否正确。

---

如果你暂时没有 API Key（或 key 留空），软件仍可基本离线运行：关键词提取会回退到本地 jieba 算法，但 **AI 精读、打分、对话、中英翻译不可用**。

### 3.5 高级选项字段说明（「高级选项 ▾」内）

「AI 模型服务」卡片底部可展开「高级选项」，以下字段**都可不填**（留空即用主模型 / 用内置默认地址）。填错不影响运行，顶多退回默认。

| 字段 | 含义 | 怎么填 |
|------|------|--------|
| **Base URL（可选）** | 自定义 API 服务地址。默认留空用各家内置官方地址；若你用的是第三方中转站/代理/自建网关，填它的完整地址（以 `/v1` 结尾） | 例如用某中转服务的 kimi 接口，就填 `https://你的地址.com/v1`。**普通用户留空即可** |
| **精排打分模型（score_model）** | 仅「AI 精排打分」用的模型，与主模型分开 | 想省 token 可填更便宜的模型（如 `deepseek-v4-flash`）；想更高质可填旗舰。**留空 = 用主模型** |
| **对话模型（chat_model）** | 仅「StudyCopilot 对话」用的模型 | 同上，可单独指定。**留空 = 用主模型** |
| **推理模型（reasoning_model）** | 开启「两步推理」：先让该模型深度推理，再用对话模型生成回复 | ⚠️ **非空才启用两步推理**，会让每次对话多调一次 API（更慢、更费 token）。**默认留空 = 单步直答（推荐）**；只有你需要更强逻辑链时才填，且需先用「测试」确认该模型支持推理输出 |

> 关键词提取、中英翻译始终走主模型，不受这三项影响。

### 3.6 配置 OpenAlex API Key（建议配置）

**这是什么？** OpenAlex 是本软件默认开启的正式论文数据源。它从 **2026 年 2 月 13 日起**调整了政策：不再支持匿名/邮箱方式的"礼貌池"访问，改为 **API Key 制**——

| 情况 | 每日额度 | 体验 |
|------|---------|------|
| 不配置 Key（默认） | **100 次/天** | 检索一两次就可能触发 429 限流，提示"⚠ OpenAlex 限流(429)" |
| 配置免费 Key 后 | **100,000 次/天** | 正常科研使用完全够用 |

> ⚠️ **这是"建议配置"而非"必须配置"**：不配置 Key 软件照样能跑（还有 arXiv、Europe PMC 两个免 Key 数据源兜底），只是在 OpenAlex 被限流当天结果会变少。想要稳定的检索体验，建议花两分钟配一个。

**配置步骤：**

1. **注册账号**：打开 https://openalex.org ，点击右上角 **Sign in / Sign up**，用邮箱注册一个免费账号（支持国内邮箱，需邮箱验证）。
2. **创建 API Key**：登录后进入个人中心（Account / Dashboard），找到 **API Key** 页面，点击创建，会生成一串密钥。
3. **粘贴到软件里**：打开 PaperPilot →「设置」页 →「数据源」卡片 → **OpenAlex API Key** 输入框（密文显示），粘贴密钥。**点输入框外任意位置或按回车即自动保存**（与其它设置一样存入本地 `config.yaml`，不会上传）。

对应的 config.yaml 写法（也可直接改文件）：

```yaml
data_sources:
  openalex_api_key: "你的OpenAlex密钥"
```

**如何确认生效**：配置 Key 前如果检索时出现"⚠ OpenAlex 限流(429)，本次结果可能不完整"，配置 Key 后重启软件再检索，该提示不再出现即说明额度已生效。

**常见疑问：**

- **Key 忘了/丢了怎么办？** 回到 OpenAlex 个人中心重新生成一个，旧 Key 自动失效，替换粘贴即可。
- **要付费吗？** 不用。免费账号即有 10 万次/天的额度，对个人科研绰绰有余。
- **换了电脑怎么办？** `config.yaml` 里的 Key 跟随文件走，把文件复制到新电脑（或重新粘贴一次）即可。

---

## 4. 首次启动

确保你已完成上述所有步骤后，在项目目录下执行：

```bash
python app.py
```

### 首次启动会发生什么

1. **窗口出现**：一个约 1200×750 的桌面窗口弹出，默认打开"文献"页面
2. **数据库创建**：项目目录下自动生成 `paperpilot.db`（SQLite 数据库文件）
3. **模型下载（首次检索时）**：

   当你第一次点击"开始检索"时，程序会自动下载以下模型文件：

   | 模型 | 大小 | 用途 | 存放位置 |
   |------|------|------|---------|
   | mxbai-rerank-base-v2 | ~942 MB | 语义精排 | `~/.cache/modelscope/` 或 HuggingFace 缓存 |
   | paraphrase-multilingual-MiniLM-L12-v2 | ~470 MB | 中文关键词提取（回退用） | 同上 |
   | all-MiniLM-L6-v2 | ~90 MB | 英文关键词提取（回退用） | 同上 |

   > 这三个模型仅在 **DeepSeek API 不可用** 或 **进行 CE 语义排序** 时才会下载。如果你已配置 API Key 且不点"CE 排序"按钮，可能永远不需要下载它们。
   >
   > 下载时间取决于网络（校园网/国内网络较慢，首次可能需要 10-30 分钟）。下载只需一次，后续使用不再重复下载。

4. **PDF.js 下载（首次阅读 PDF 时）**：

   第一次点击"阅读原文"时，程序会自动下载 PDF.js 渲染引擎（~3 MB），缓存到 `~/.paperpilot_pdfjs/` 目录。之后即使断网也能正常使用 PDF 阅读器。

### 启动成功标志

看到以下界面即表示启动成功：

- 顶部导航栏有"文献 / 检索 / 设置"三个标签
- 右侧有"StudyCopilot"对话面板

---

## 5. 功能详解

### 5.1 检索论文

这是核心工作流的第一步，帮助你从海量论文中找到与课题相关的文献。

**操作步骤：**

1. 点击顶部导航栏的 **"检索"** 标签
2. 在 **课题名称** 输入框填写研究课题的简短名称（例如"钙钛矿太阳能电池稳定性"）
3. 在 **课题描述** 输入框填写 1–3 句话描述你的研究方向（越具体越好）
4. 点击 **"提取关键词"** 按钮——AI 会自动分析你的课题描述，提取出技术关键词
5. 提取完成后，关键词会出现在三个拖拽区中：
   - **主关键词**（星标）：检索的核心词，必须命中
   - **副关键词**（箭头）：辅助检索词
   - **普通关键词**：补充召回
6. 你可以**拖拽关键词 Chip** 在三个区域之间移动，调整它们的重要性。也可以手动添加新的关键词
7. 点击 **"开始检索"** 按钮

**检索过程：**

- 程序先将中文关键词翻译为英文
- 然后分别从已启用的数据源（arXiv / OpenAlex / Europe PMC）检索
- 各来源结果去重合并
- 最后用 Cross-Encoder 语义模型精排

**检索结果：**

默认每个数据源检索 **250 篇**（可在设置中调整）。结果列表显示每篇论文的标题、作者、年份、来源、引用次数、CE 得分和文章类型（综述/研究论文）。你可以：

- 点击任意一篇论文查看详情（右侧侧边栏弹出）
- 点击表头（标题/作者/年份/引用/得分）进行排序
- 使用 **"多选"** 模式勾选感兴趣的论文
- 点击 **"保存到文献库"** 将结果存入某个课题

**AI 精排（可选）：**

检索完成后，你可以点击 **"AI 精排"** 按钮，让 AI 基于摘要对结果进行四维打分（相关性 × 方法质量 × 创新性 × 时效性），输出 0-100 分的综合评分和 S/A/B/C/D 档位，帮助你快速锁定最值得读的论文。

> AI 精排需要 DeepSeek API Key，按 token 计费，每篇论文约消耗 ~120 tokens。

### 5.2 管理文献库

点击顶部 **"文献"** 标签进入文献管理页面。

**课题管理：**

- 左侧显示所有课题列表，点击可切换
- 点击 **"+"** 按钮新建课题
- 点击课题旁的菜单进行重命名或删除
- 删除课题时 PDF 文件会进入回收站（7 天后自动清理）

**论文管理：**

- 论文列表显示标题、作者、年份、AI 得分、CE 得分、阅读状态
- 状态筛选：全部 / 未读 / 略读 / 精读
- 点击状态指示灯切换阅读状态（未读→略读→精读→未读）
- 点击论文标题查看完整详情（弹出对话框，含摘要、AI 评分各维度理由、AI 精读笔记等）
- 已下载 PDF 的论文序号和标题会显示为**绿色**，方便识别
- 点击 **阅读按钮**（📖）打开 PDF 阅读器
- 点击 **AI 精读按钮**（🧠）启动 AI 结构化分析
- 点击 **删除按钮**（🗑）移除论文

**导入本地 PDF：**

- 点击 **上传按钮**（📤），可以选择：
  - 选择单个 PDF 文件
  - 选择多个 PDF 文件
  - 选择整个文件夹（自动递归扫描所有 PDF）
- 导入时程序会用 PyMuPDF 自动提取标题（支持按 PDF 内最大字号定位）、作者、摘要和年份
- 扫描件（纯图片无文字层的 PDF）会被自动跳过
- 编码乱码的 PDF metadata 标题会被自动过滤并回退到字号检测

**排序：**

- 点击 **排序按钮**（⇅）用 Cross-Encoder 对课题内论文重新进行语义排序
- 点击 **AI 排序按钮**（📊）用 AI 基于摘要对论文打分排序
- 两种排序结果都会持久化保存到数据库

**多选与对比：**

- 点击 **"多选"** 进入多选模式，勾选论文后可批量删除或**对比分析**（选中 2 篇以上后点击对比按钮，Agent 会从研究目标、方法、发现、创新点、局限性五个维度进行对比）
- 在文献库多选论文后，Agent 对话面板也会自动将选中论文作为上下文

**保存机制：**

- 检索结果保存到文献库时，仅保存**当前显示的前 N 篇**（即排序后的 top 结果），而非全部检索到的论文
- 保存后程序会**后台自动下载**可获取的 PDF，无需手动操作

### 5.3 AI 精读与对话

**AI 精读一篇论文：**

在文献库中点击论文旁的 **🧠 按钮**，AI 会：

1. 自动获取论文全文（优先 PDF → 网页 HTML → 摘要回退）
2. 用 RLM 三层阅读策略分析：
   - 短文本（<8K 字符）：直接全文分析
   - 中等文本（8K–60K）：滑动窗口分段阅读
   - 长文本（>60K）：切块独立分析后合成
3. 输出结构化分析报告：
   - 核心贡献：论文解决的关键问题
   - 研究方法：技术路线
   - 关键证据：支撑结论的实验/数据
   - 创新亮点
   - 局限不足
   - 评分：新颖性 / 严谨性 / 重要性（各 1-10 分）

分析结果会展示在右侧 StudyCopilot 面板中，同时保存到数据库和本地 `outputs/deep_read/` 目录。

**AI 对话助手（StudyCopilot）：**

右侧常驻的 **StudyCopilot** 面板是一个课题上下文感知的学术对话助手，支持 **Markdown 富文本渲染**（标题、粗体、列表、表格、代码块）。

- 选择一个课题后，助手会自动加载该课题的论文信息
- 你可以直接问："这篇论文的方法有什么亮点？" / "对比一下前两篇论文" / "这个研究方向还有哪些值得关注的子方向"
- 如果你在消息中 **@论文标题** 或包含论文标题关键词，助手会自动引用该论文的详细信息
- **Agent 可主动执行操作**：在对话中可以要求 Agent "把这个课题改名为xxx" / "把课题描述改成xxx" / "开始新的检索"，Agent 会自动弹出确认对话框并执行
- **面板可拖拽调整宽度**：拖动 Agent 面板左边缘可调整宽度
- 对话历史会自动保存，即使关闭程序也不会丢失
- 当对话过长（超过约 80K token）时，旧消息会自动压缩为摘要，不影响后续对话质量

### 5.4 阅读原文

点击论文旁的 **📖 按钮**，程序会按以下优先级尝试获取全文：

1. **本地 PDF 文件**：如果你已导入 PDF，直接打开
2. **课题仓库**：查找该论文是否已在其他课题中导入过
3. **下载缓存**：查找之前是否通过检索页下载过
4. **自动下载**：对于 arXiv、Nature、Springer 等期刊，尝试直链下载 PDF（优先使用 `curl_cffi` 浏览器指纹伪装，未安装时自动降级为普通请求）
5. **无法获取**：弹出对话框，引导你手动下载

打开后，PDF 会在一个**独立窗口**中渲染：

- 支持鼠标滚轮滚动翻页
- 支持文字选择（拖动选中，Ctrl+C 复制）
- 顶部导航栏显示当前页码，可跳转到指定页
- 支持上一页 / 下一页按钮
- 主题颜色跟随主界面设置（夜间模式自动适配深色背景）

> PDF 阅读器基于 PDF.js 3.11，首次使用时自动下载 ~3MB 文件到本地，之后无需网络。阅读器使用 Edge WebView2 渲染（Windows 10/11 自带）。

### 5.5 导出文献

在文献管理页面，点击工具栏的 **下载按钮**（📥），可以选择：

| 格式 | 说明 |
|------|------|
| **BibTeX** | LaTeX 文献引用格式，可直接用于 Overleaf / 本地 LaTeX 项目 |
| **CSV** | Excel 表格格式，含标题、作者、年份、来源、期刊、引用数、DOI、URL |

导出时会弹出保存对话框，让你选择保存位置和文件名。如果是多选模式下导出，只导出选中的论文。

### 5.6 设置页面

点击顶部 **"设置"** 标签进入设置页面。主要分区如下：

| 设置项 | 说明 |
|--------|------|
| **AI 模型服务** | 选择 Provider、填 API Key、选/输模型名，可「测试」「保存」；底部「高级选项」含 Base URL、任务专用模型。**各家 Key 怎么拿 + 高级字段填法详见 §3.4 / §3.5** |
| **数据源** | 开关 arXiv / OpenAlex / Europe PMC 检索；**OpenAlex API Key** 输入框（密文，建议配置，申请步骤见 §3.6） |
| **检索数量** | 每个来源的最大检索结果数（100–500 篇，默认 250） |
| **显示/精排数量** | 最终显示论文数（10–200）和送入精排的候选数（10–200） |
| **配色主题** | 6 种主题色：薄荷绿 / 海蓝 / 暖沙 / 暮紫 / 玫瑰 / 青碧 |
| **夜间模式** | 切换深色/浅色界面 |

所有设置自动保存到 `config.yaml`，下次启动自动恢复。其中「AI 模型服务」的配置流程、各家 Key 获取方式及高级字段含义，详见本章 §3.4「配置 AI 模型服务（API Key）」与 §3.5「高级选项字段说明」。

---

## 6. 常见问题

### Q1：启动时报错 "No module named 'xxx'"

依赖没有完整安装。执行：

```bash
pip install -r requirements.txt
```

如果某个包安装失败，尝试单独安装它：

```bash
pip install 包名
```

### Q2：点击"提取关键词"后卡住不动

可能是 DeepSeek API Key 未配置或网络不通。

- 检查 `config.yaml` 中的 `api_key` 是否正确
- 如果确实没有 API Key，把 `api_key` 留空，程序会回退到本地算法（需要下载模型，首次较慢）

### Q3：检索论文时提示"未找到相关论文"

- 检查网络连接（arXiv、OpenAlex、Europe PMC 都需要网络）
- **看提示里有没有"限流(429)"字样**：若提示 OpenAlex 被限流，说明当日 100 次免费额度已用完，按 [§3.6](#36-配置-openalex-api-key建议配置) 配置免费 API Key 即可（10 万次/天）；若是 arXiv/Europe PMC 限流，稍等几分钟后重试
- 尝试在设置中减少"精排候选"数量
- 尝试在设置中增加每个数据源最大检索数量（检索数量建议选 250 篇或以上，不建议小于 100 篇）
- 检查关键词翻译是否正确（中文关键词需要有对应的英文翻译）
- 尝试只开启一个数据源（先只用 openalex 试试）

### Q4：Cross-Encoder 模型下载太久/失败

这通常是因为网络到 HuggingFace 不通畅。可以尝试以下方法：

- **方法一**：设置 HuggingFace 镜像（国内用户推荐）
  在命令行执行：
  ```bash
  set HF_ENDPOINT=https://hf-mirror.com
  ```
  然后重启程序。

- **方法二**：手动下载模型
  1. 从 https://hf-mirror.com/mixedbread-ai/mxbai-rerank-base-v2 下载所有文件
  2. 放入 `C:\Users\你的用户名\.cache\modelscope\mixedbread-ai\mxbai-rerank-base-v2\` 目录

- **方法三**：不用 CE 排序
  程序在 CE 超时后会**自动回退到 API 分数排序**，不影响使用。你可以在设置中将"精排候选"调低。

### Q5：PDF 阅读器打不开/闪退

- 阅读器使用 Edge WebView2 运行时（Windows 11 自带，Windows 10 可能需要手动安装）：
  下载地址：https://developer.microsoft.com/en-us/microsoft-edge/webview2/
- 确认已安装 `pywebview`：`pip install pywebview>=4.0`

### Q6：Wiley / ACS / IEEE 等期刊的 PDF 下载不了

这些期刊有付费墙，直链下载只对**你所在机构已订阅**的期刊有效。arXiv 论文通常可以免费下载。

- 如果安装了 `curl_cffi`（`pip install curl_cffi`），程序会使用更强的浏览器指纹伪装，有助于部分出版商的 PDF 下载
- 解决方法：在浏览器中登录学校/机构的 VPN 或图书馆代理，手动下载 PDF，然后通过文献库的"导入 PDF"功能添加到 PaperPilot

### Q7：程序占了 2GB+ 内存

这是正常的，主要内存占用来自：

- Cross-Encoder 模型（~942 MB），在检索和 CE 排序时会加载
- PyTorch 运行时（~500 MB）
- PDF 阅读器窗口（~200 MB）

如果内存紧张，检索完成后程序会自动释放 Cross-Encoder 模型。你可以在设置中将"精排候选"数调低以减少内存峰值。

### Q8：如何更新到最新版本？

如果你使用 Git 克隆的：

```bash
git pull origin main
pip install -r requirements.txt  # 更新依赖（如有变化）
```

如果是 ZIP 下载的：重新下载最新 ZIP 并覆盖，注意保留你的 `config.yaml`（不要覆盖它）。

### Q9：数据库文件在哪里？可以备份吗？

数据库文件在项目根目录下的 `paperpilot.db`。直接复制这个文件即可备份。

论文 PDF 存储在项目根目录下的 `repository/` 文件夹中，按课题名称分目录存放。

### Q10：如何完全卸载？

删除项目文件夹即可。此外以下目录也可能存有缓存文件，可手动删除：

- `C:\Users\你的用户名\.paperpilot_pdf_cache\`（PDF 下载缓存）
- `C:\Users\你的用户名\.paperpilot_pdfjs\`（PDF.js 引擎）
- `C:\Users\你的用户名\.paperpilot_html_cache\`（HTML 全文缓存）
- `C:\Users\你的用户名\.cache\modelscope\`（AI 模型缓存，与其他项目共享）

---

> 如有其他问题或功能建议，请在 GitHub 提交 Issue：
> https://github.com/Dyotropic/PaperPilot/issues
